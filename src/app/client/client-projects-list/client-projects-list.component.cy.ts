import { ClientProjectsListComponent } from './client-projects-list.component'

describe('ClientProjectsListComponent', () => {
  it('should mount', () => {
    cy.mount(ClientProjectsListComponent)
  })
})