import { ClientProjectMembersComponent } from './client-project-members.component'

describe('ClientProjectMembersComponent', () => {
  it('should mount', () => {
    cy.mount(ClientProjectMembersComponent)
  })
})